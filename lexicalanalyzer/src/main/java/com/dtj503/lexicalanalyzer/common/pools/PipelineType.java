package com.dtj503.lexicalanalyzer.common.pools;

/**
 * Enum for the two pipeline model types.
 *
 * TODO review if this is needed - it currently only utilises a pipeline with one set of annotations but could become
 *      useful
 *
 * @author Dan Jackson (dtj503@york.ac.uk)
 */
public enum PipelineType {

    FAST

}
